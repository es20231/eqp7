interface UploadImageProps {
  userId: string
  token: string
}

const UploadImage = ({ userId, token }: UploadImageProps) => {
  return (
    <div>
      <h1>UploadImage</h1>
    </div>
  )
}

export { UploadImage }
