import { describe, expect, it } from 'vitest';

describe('test', () => {
  it('should be true', () => {
      expect(true).toBe(true);
  })

  it('should be false', () => {
    expect(false).toBe(false);
  })
})